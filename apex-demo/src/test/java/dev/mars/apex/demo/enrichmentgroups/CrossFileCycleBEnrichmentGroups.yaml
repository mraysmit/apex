# ============================================================================
# Cross-file cycle group B (references group A)
# ============================================================================
metadata:
  id: "cf-cycle-b-enrichment-groups"
  name: "Cross-file Cycle B Enrichment Groups"
  version: "1.0.0"
  type: "rule-config"
  description: "Cycle B enrichment groups for cross-file circular dependency testing"
  author: "APEX Demo Team"
  created-date: "2025-01-15"
  tags: ["demo", "enrichment-groups", "cross-file", "cycle-b"]

enrichment-groups:
  - id: gB
    name: Group B (cycles to A)
    operator: AND
    enrichment-ids: [ e2 ]
    enrichment-group-references: [ gA ]

