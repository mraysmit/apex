# ============================================================================
# Enrichment Groups (separate file) for negative validation test
# ============================================================================
metadata:
  id: "neg-missing-enrichment-groups"
  name: "Negative: Missing Enrichment (Groups file)"
  version: "1.0.0"
  type: "rule-config"
  description: "Negative test case for missing enrichment group validation"
  author: "APEX Demo Team"
  created-date: "2025-01-15"
  tags: ["demo", "enrichment-groups", "negative-test", "validation"]

enrichment-groups:
  - id: base_and
    name: Base AND (invalid)
    operator: AND
    stop-on-first-failure: true
    enrichment-ids: [ e1, e9 ]  # e9 does not exist in enrichments

