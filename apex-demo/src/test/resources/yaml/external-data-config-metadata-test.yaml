metadata:
  id: "external-data-config-metadata-test"
  name: "External Data Config Metadata Validation Test"
  version: "1.0.0"
  description: "Test external-data-config document metadata validation requirements"
  type: "external-data-config"
  author: "apex-test-suite@example.com"
  created-date: "2025-09-14"
  tags: ["apex-test", "external-data-config", "metadata-validation"]

dataSources:
  - name: "minimal-database"
    type: "database"
    sourceType: "h2"
    enabled: true
    description: "Minimal database configuration for metadata validation"
    
    connection:
      host: "localhost"
      port: 9092
      database: "metadata_test"
      username: "sa"
      password: ""
      
    queries:
      getRecord: "SELECT * FROM test_records WHERE id = :id"

configuration:
  defaultConnectionTimeout: 30000
  
  monitoring:
    enabled: true
    healthCheckLogging: true
