metadata:
  name: "Bootstrap Document Metadata Validation Test"
  version: "1.0.0"
  description: "Test bootstrap document metadata validation with all required fields"
  type: "bootstrap"
  id: "bootstrap-metadata-test"
  business-domain: "Testing"
  created-by: "apex-test-suite@example.com"
  created: "2025-09-14"
  last-modified: "2025-09-14"
  tags: ["test", "metadata", "validation", "bootstrap"]

bootstrap:
  # Minimal bootstrap configuration for metadata validation
  demo-scenarios:
    - scenario-id: "metadata-validation-test-scenario"
      description: "Simple scenario for testing metadata validation requirements"
      data-types: ["TestDataType"]
      business-domain: "Testing"
      enabled: true

  infrastructure:
    minimal-setup: true
    validation-only: true

data-sources:
  # Minimal data source configuration for metadata validation
  test-database:
    type: "h2"
    url: "jdbc:h2:mem:metadata_validation_test"
    username: "sa"
    password: ""
