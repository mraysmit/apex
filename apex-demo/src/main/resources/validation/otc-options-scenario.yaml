metadata:
  id: "otc-options-processing-scenario"
  name: "OTC Options Processing Scenario"
  version: "1.0.0"
  description: "Scenario configuration that associates OTC Options data types with existing rule configuration files for processing"
  type: "scenario"
  author: "apex.demo.team@company.com"
  business-domain: "OTC Options Processing"
  owner: "apex.demo.team@company.com"
  created-date: "2024-12-24"
  tags: ["apex-demo", "validation", "otc-options", "scenario"]

# ============================================================================
# OTC OPTIONS PROCESSING SCENARIO
# ============================================================================
#
# This scenario configuration associates OTC Options data types with
# existing rule configuration files for processing.
#
# PURPOSE:
# - Maps OTC Options data types to existing rule configurations
# - References existing YAML rule files for validation and enrichment
# - Lightweight routing configuration
#
# ============================================================================

# ============================================================================
# SCENARIO CONFIGURATION
# ============================================================================

scenario:
  scenario-id: "otc-options-processing-scenario"
  name: "OTC Options Standard Processing"
  description: "Associates OTC Options with existing rule configurations"

  # Data types this scenario applies to
  data-types:
    - "dev.mars.apex.demo.model.OtcOption"
    - "OtcOption"  # Short name alias

  # References to existing rule configuration files
  rule-configurations:
    - "bootstrap/otc-options-bootstrap.yaml"
    - "config/derivatives-validation-rules.yaml"

