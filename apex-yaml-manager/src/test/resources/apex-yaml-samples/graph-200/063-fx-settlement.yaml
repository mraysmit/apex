metadata:
  id: fx-settlement
  type: rule-config
  version: "1.0"
  description: "FX settlement rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: fx-settlement-group
    description: "FX settlement rules group"
    rule-ids:
      - "fx-settlement-rule-1"
      - "fx-settlement-rule-2"
      - "fx-settlement-rule-3"

rules:
  - id: fx-settlement-rule-1
    description: "Rule 1 for FX settlement rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: fx-settlement-rule-2
    description: "Rule 2 for FX settlement rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: fx-settlement-rule-3
    description: "Rule 3 for FX settlement rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "111-timestamp-rules.yaml"
  - "112-currency-rules.yaml"
