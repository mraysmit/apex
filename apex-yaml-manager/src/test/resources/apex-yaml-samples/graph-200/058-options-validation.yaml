metadata:
  id: options-validation
  type: rule-config
  version: "1.0"
  description: "Options validation rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: options-validation-group
    description: "Options validation rules group"
    rule-ids:
      - "options-validation-rule-1"
      - "options-validation-rule-2"
      - "options-validation-rule-3"

rules:
  - id: options-validation-rule-1
    description: "Rule 1 for Options validation rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: options-validation-rule-2
    description: "Rule 2 for Options validation rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: options-validation-rule-3
    description: "Rule 3 for Options validation rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "100-common-validation-rules.yaml"
