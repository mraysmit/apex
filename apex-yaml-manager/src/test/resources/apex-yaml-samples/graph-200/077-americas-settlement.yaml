metadata:
  id: americas-settlement
  type: rule-config
  version: "1.0"
  description: "Americas settlement rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: americas-settlement-group
    description: "Americas settlement rules group"
    rule-ids:
      - "americas-settlement-rule-1"
      - "americas-settlement-rule-2"
      - "americas-settlement-rule-3"

rules:
  - id: americas-settlement-rule-1
    description: "Rule 1 for Americas settlement rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: americas-settlement-rule-2
    description: "Rule 2 for Americas settlement rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: americas-settlement-rule-3
    description: "Rule 3 for Americas settlement rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "121-regional-rules.yaml"
