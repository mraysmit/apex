metadata:
  id: bond-pricing
  type: rule-config
  version: "1.0"
  description: "Bond pricing rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: bond-pricing-group
    description: "Bond pricing rules group"
    rule-ids:
      - "bond-pricing-rule-1"
      - "bond-pricing-rule-2"
      - "bond-pricing-rule-3"

rules:
  - id: bond-pricing-rule-1
    description: "Rule 1 for Bond pricing rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: bond-pricing-rule-2
    description: "Rule 2 for Bond pricing rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: bond-pricing-rule-3
    description: "Rule 3 for Bond pricing rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "113-pricing-rules.yaml"
