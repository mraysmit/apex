metadata:
  id: timestamp-rules
  type: rule-config
  version: "1.0"
  description: "Timestamp validation rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: timestamp-rules-group
    description: "Timestamp validation rules group"
    rule-ids:
      - "timestamp-rules-rule-1"
      - "timestamp-rules-rule-2"
      - "timestamp-rules-rule-3"
      - "timestamp-rules-rule-4"

rules:
  - id: timestamp-rules-rule-1
    description: "Rule 1 for Timestamp validation rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: timestamp-rules-rule-2
    description: "Rule 2 for Timestamp validation rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: timestamp-rules-rule-3
    description: "Rule 3 for Timestamp validation rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"
  - id: timestamp-rules-rule-4
    description: "Rule 4 for Timestamp validation rules"
    condition: "trade.field4 != null && trade.field4 > 0"
    severity: "ERROR"
