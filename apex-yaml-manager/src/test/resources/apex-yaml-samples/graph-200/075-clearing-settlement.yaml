metadata:
  id: clearing-settlement
  type: rule-config
  version: "1.0"
  description: "Clearing settlement rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: clearing-settlement-group
    description: "Clearing settlement rules group"
    rule-ids:
      - "clearing-settlement-rule-1"
      - "clearing-settlement-rule-2"
      - "clearing-settlement-rule-3"

rules:
  - id: clearing-settlement-rule-1
    description: "Rule 1 for Clearing settlement rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: clearing-settlement-rule-2
    description: "Rule 2 for Clearing settlement rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: clearing-settlement-rule-3
    description: "Rule 3 for Clearing settlement rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "120-clearing-rules.yaml"
