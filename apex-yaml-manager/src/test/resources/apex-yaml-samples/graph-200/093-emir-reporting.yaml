metadata:
  id: emir-reporting
  type: rule-config
  version: "1.0"
  description: "EMIR reporting rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: emir-reporting-group
    description: "EMIR reporting rules group"
    rule-ids:
      - "emir-reporting-rule-1"
      - "emir-reporting-rule-2"
      - "emir-reporting-rule-3"

rules:
  - id: emir-reporting-rule-1
    description: "Rule 1 for EMIR reporting rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: emir-reporting-rule-2
    description: "Rule 2 for EMIR reporting rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: emir-reporting-rule-3
    description: "Rule 3 for EMIR reporting rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "127-reporting-base.yaml"
