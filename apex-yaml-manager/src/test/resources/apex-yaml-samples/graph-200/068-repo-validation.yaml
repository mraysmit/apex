metadata:
  id: repo-validation
  type: rule-config
  version: "1.0"
  description: "Repo validation rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: repo-validation-group
    description: "Repo validation rules group"
    rule-ids:
      - "repo-validation-rule-1"
      - "repo-validation-rule-2"
      - "repo-validation-rule-3"

rules:
  - id: repo-validation-rule-1
    description: "Rule 1 for Repo validation rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: repo-validation-rule-2
    description: "Rule 2 for Repo validation rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: repo-validation-rule-3
    description: "Rule 3 for Repo validation rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "100-common-validation-rules.yaml"
