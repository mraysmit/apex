metadata:
  id: liquidity-base
  type: rule-config
  version: "1.0"
  description: "Liquidity management base"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: liquidity-base-group
    description: "Liquidity management base group"
    rule-ids:
      - "liquidity-base-rule-1"
      - "liquidity-base-rule-2"
      - "liquidity-base-rule-3"
      - "liquidity-base-rule-4"

rules:
  - id: liquidity-base-rule-1
    description: "Rule 1 for Liquidity management base"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: liquidity-base-rule-2
    description: "Rule 2 for Liquidity management base"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: liquidity-base-rule-3
    description: "Rule 3 for Liquidity management base"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"
  - id: liquidity-base-rule-4
    description: "Rule 4 for Liquidity management base"
    condition: "trade.field4 != null && trade.field4 > 0"
    severity: "ERROR"

dependencies:
  - "114-risk-rules.yaml"
