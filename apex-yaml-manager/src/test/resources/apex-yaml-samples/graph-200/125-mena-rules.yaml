metadata:
  id: mena-rules
  type: rule-config
  version: "1.0"
  description: "MENA specific rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: mena-rules-group
    description: "MENA specific rules group"
    rule-ids:
      - "mena-rules-rule-1"
      - "mena-rules-rule-2"
      - "mena-rules-rule-3"
      - "mena-rules-rule-4"

rules:
  - id: mena-rules-rule-1
    description: "Rule 1 for MENA specific rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: mena-rules-rule-2
    description: "Rule 2 for MENA specific rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: mena-rules-rule-3
    description: "Rule 3 for MENA specific rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"
  - id: mena-rules-rule-4
    description: "Rule 4 for MENA specific rules"
    condition: "trade.field4 != null && trade.field4 > 0"
    severity: "ERROR"

dependencies:
  - "121-regional-rules.yaml"
