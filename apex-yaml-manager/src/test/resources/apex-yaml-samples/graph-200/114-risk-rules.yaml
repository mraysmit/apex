metadata:
  id: risk-rules
  type: rule-config
  version: "1.0"
  description: "Risk calculation rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: risk-rules-group
    description: "Risk calculation rules group"
    rule-ids:
      - "risk-rules-rule-1"
      - "risk-rules-rule-2"
      - "risk-rules-rule-3"
      - "risk-rules-rule-4"

rules:
  - id: risk-rules-rule-1
    description: "Rule 1 for Risk calculation rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: risk-rules-rule-2
    description: "Rule 2 for Risk calculation rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: risk-rules-rule-3
    description: "Rule 3 for Risk calculation rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"
  - id: risk-rules-rule-4
    description: "Rule 4 for Risk calculation rules"
    condition: "trade.field4 != null && trade.field4 > 0"
    severity: "ERROR"

dependencies:
  - "110-reference-data-rules.yaml"
