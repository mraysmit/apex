metadata:
  id: fx-validation
  type: rule-config
  version: "1.0"
  description: "FX validation rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: fx-validation-group
    description: "FX validation rules group"
    rule-ids:
      - "fx-validation-rule-1"
      - "fx-validation-rule-2"
      - "fx-validation-rule-3"

rules:
  - id: fx-validation-rule-1
    description: "Rule 1 for FX validation rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: fx-validation-rule-2
    description: "Rule 2 for FX validation rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: fx-validation-rule-3
    description: "Rule 3 for FX validation rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "100-common-validation-rules.yaml"
