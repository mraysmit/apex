metadata:
  id: equity-settlement
  type: rule-config
  version: "1.0"
  description: "Equity settlement rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: equity-settlement-group
    description: "Equity settlement rules group"
    rule-ids:
      - "equity-settlement-rule-1"
      - "equity-settlement-rule-2"
      - "equity-settlement-rule-3"

rules:
  - id: equity-settlement-rule-1
    description: "Rule 1 for Equity settlement rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: equity-settlement-rule-2
    description: "Rule 2 for Equity settlement rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: equity-settlement-rule-3
    description: "Rule 3 for Equity settlement rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "111-timestamp-rules.yaml"
  - "112-currency-rules.yaml"
