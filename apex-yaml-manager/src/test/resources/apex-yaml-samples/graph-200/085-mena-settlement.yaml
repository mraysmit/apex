metadata:
  id: mena-settlement
  type: rule-config
  version: "1.0"
  description: "MENA settlement rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: mena-settlement-group
    description: "MENA settlement rules group"
    rule-ids:
      - "mena-settlement-rule-1"
      - "mena-settlement-rule-2"
      - "mena-settlement-rule-3"

rules:
  - id: mena-settlement-rule-1
    description: "Rule 1 for MENA settlement rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: mena-settlement-rule-2
    description: "Rule 2 for MENA settlement rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: mena-settlement-rule-3
    description: "Rule 3 for MENA settlement rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "125-mena-rules.yaml"
