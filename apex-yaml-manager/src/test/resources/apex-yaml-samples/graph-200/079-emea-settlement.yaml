metadata:
  id: emea-settlement
  type: rule-config
  version: "1.0"
  description: "EMEA settlement rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: emea-settlement-group
    description: "EMEA settlement rules group"
    rule-ids:
      - "emea-settlement-rule-1"
      - "emea-settlement-rule-2"
      - "emea-settlement-rule-3"

rules:
  - id: emea-settlement-rule-1
    description: "Rule 1 for EMEA settlement rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: emea-settlement-rule-2
    description: "Rule 2 for EMEA settlement rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: emea-settlement-rule-3
    description: "Rule 3 for EMEA settlement rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "122-emea-rules.yaml"
