metadata:
  id: operational-base
  type: rule-config
  version: "1.0"
  description: "Operational risk base"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: operational-base-group
    description: "Operational risk base group"
    rule-ids:
      - "operational-base-rule-1"
      - "operational-base-rule-2"
      - "operational-base-rule-3"
      - "operational-base-rule-4"

rules:
  - id: operational-base-rule-1
    description: "Rule 1 for Operational risk base"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: operational-base-rule-2
    description: "Rule 2 for Operational risk base"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: operational-base-rule-3
    description: "Rule 3 for Operational risk base"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"
  - id: operational-base-rule-4
    description: "Rule 4 for Operational risk base"
    condition: "trade.field4 != null && trade.field4 > 0"
    severity: "ERROR"

dependencies:
  - "110-reference-data-rules.yaml"
