metadata:
  id: apac-settlement
  type: rule-config
  version: "1.0"
  description: "APAC settlement rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: apac-settlement-group
    description: "APAC settlement rules group"
    rule-ids:
      - "apac-settlement-rule-1"
      - "apac-settlement-rule-2"
      - "apac-settlement-rule-3"

rules:
  - id: apac-settlement-rule-1
    description: "Rule 1 for APAC settlement rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: apac-settlement-rule-2
    description: "Rule 2 for APAC settlement rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: apac-settlement-rule-3
    description: "Rule 3 for APAC settlement rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "123-apac-rules.yaml"
