metadata:
  id: commodity-validation
  type: rule-config
  version: "1.0"
  description: "Commodity validation rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: commodity-validation-group
    description: "Commodity validation rules group"
    rule-ids:
      - "commodity-validation-rule-1"
      - "commodity-validation-rule-2"
      - "commodity-validation-rule-3"

rules:
  - id: commodity-validation-rule-1
    description: "Rule 1 for Commodity validation rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: commodity-validation-rule-2
    description: "Rule 2 for Commodity validation rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: commodity-validation-rule-3
    description: "Rule 3 for Commodity validation rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "100-common-validation-rules.yaml"
