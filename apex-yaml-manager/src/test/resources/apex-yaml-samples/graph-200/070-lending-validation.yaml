metadata:
  id: lending-validation
  type: rule-config
  version: "1.0"
  description: "Securities lending validation"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: lending-validation-group
    description: "Securities lending validation group"
    rule-ids:
      - "lending-validation-rule-1"
      - "lending-validation-rule-2"
      - "lending-validation-rule-3"

rules:
  - id: lending-validation-rule-1
    description: "Rule 1 for Securities lending validation"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: lending-validation-rule-2
    description: "Rule 2 for Securities lending validation"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: lending-validation-rule-3
    description: "Rule 3 for Securities lending validation"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "100-common-validation-rules.yaml"
