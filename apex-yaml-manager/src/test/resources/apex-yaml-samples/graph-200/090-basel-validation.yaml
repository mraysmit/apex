metadata:
  id: basel-validation
  type: rule-config
  version: "1.0"
  description: "Basel validation rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: basel-validation-group
    description: "Basel validation rules group"
    rule-ids:
      - "basel-validation-rule-1"
      - "basel-validation-rule-2"
      - "basel-validation-rule-3"

rules:
  - id: basel-validation-rule-1
    description: "Rule 1 for Basel validation rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: basel-validation-rule-2
    description: "Rule 2 for Basel validation rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: basel-validation-rule-3
    description: "Rule 3 for Basel validation rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "126-regulatory-base.yaml"
