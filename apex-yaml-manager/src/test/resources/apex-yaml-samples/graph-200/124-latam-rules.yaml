metadata:
  id: latam-rules
  type: rule-config
  version: "1.0"
  description: "LATAM specific rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: latam-rules-group
    description: "LATAM specific rules group"
    rule-ids:
      - "latam-rules-rule-1"
      - "latam-rules-rule-2"
      - "latam-rules-rule-3"
      - "latam-rules-rule-4"

rules:
  - id: latam-rules-rule-1
    description: "Rule 1 for LATAM specific rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: latam-rules-rule-2
    description: "Rule 2 for LATAM specific rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: latam-rules-rule-3
    description: "Rule 3 for LATAM specific rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"
  - id: latam-rules-rule-4
    description: "Rule 4 for LATAM specific rules"
    condition: "trade.field4 != null && trade.field4 > 0"
    severity: "ERROR"

dependencies:
  - "121-regional-rules.yaml"
