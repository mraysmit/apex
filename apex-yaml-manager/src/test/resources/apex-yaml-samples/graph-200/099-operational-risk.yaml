metadata:
  id: operational-risk
  type: rule-config
  version: "1.0"
  description: "Operational risk rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: operational-risk-group
    description: "Operational risk rules group"
    rule-ids:
      - "operational-risk-rule-1"
      - "operational-risk-rule-2"
      - "operational-risk-rule-3"

rules:
  - id: operational-risk-rule-1
    description: "Rule 1 for Operational risk rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: operational-risk-rule-2
    description: "Rule 2 for Operational risk rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: operational-risk-rule-3
    description: "Rule 3 for Operational risk rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "131-operational-base.yaml"
