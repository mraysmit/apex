metadata:
  id: lending-settlement
  type: rule-config
  version: "1.0"
  description: "Securities lending settlement"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: lending-settlement-group
    description: "Securities lending settlement group"
    rule-ids:
      - "lending-settlement-rule-1"
      - "lending-settlement-rule-2"
      - "lending-settlement-rule-3"

rules:
  - id: lending-settlement-rule-1
    description: "Rule 1 for Securities lending settlement"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: lending-settlement-rule-2
    description: "Rule 2 for Securities lending settlement"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: lending-settlement-rule-3
    description: "Rule 3 for Securities lending settlement"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "118-collateral-rules.yaml"
