metadata:
  id: fixed-income-scenario
  type: scenario
  version: "1.0"
  description: "Fixed income trading scenario"
  created-date: "2025-10-20"
  author: "APEX Test Generator"

# File-based dependencies following APEX patterns
rule-configurations:
  - "054-fixed-income-validation.yaml"
  - "055-fixed-income-risk.yaml"
enrichment-refs:
  - "153-yield-curve-enrichment.yaml"
  - "154-credit-rating-enrichment.yaml"
config-files:
  - "252-bond-data-config.yaml"
  - "253-rating-data-config.yaml"
scenario:
  scenario-id: "fixed-income-scenario"
  description: "Fixed income trading scenario"
  
  classification-rule:
    condition: "#dataType != null && #dataType == 'TRADE'"
    description: "Route trade data to processing"
  
  processing-stages:
    - stage-name: "validation"
      description: "Validate incoming data"
      config-file: "validation-rules.yaml"
      execution-order: 1
      failure-policy: "terminate"
    - stage-name: "enrichment"
      description: "Enrich with reference data"
      config-file: "enrichment-rules.yaml"
      execution-order: 2
      failure-policy: "continue"
  
  data-types:
    - "Trade"
    - "java.util.Map"
