metadata:
  id: americas-validation
  type: rule-config
  version: "1.0"
  description: "Americas validation rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: americas-validation-group
    description: "Americas validation rules group"
    rule-ids:
      - "americas-validation-rule-1"
      - "americas-validation-rule-2"
      - "americas-validation-rule-3"

rules:
  - id: americas-validation-rule-1
    description: "Rule 1 for Americas validation rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: americas-validation-rule-2
    description: "Rule 2 for Americas validation rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: americas-validation-rule-3
    description: "Rule 3 for Americas validation rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "100-common-validation-rules.yaml"
  - "121-regional-rules.yaml"
