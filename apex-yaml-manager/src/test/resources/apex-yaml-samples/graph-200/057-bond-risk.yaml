metadata:
  id: bond-risk
  type: rule-config
  version: "1.0"
  description: "Bond risk calculation rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: bond-risk-group
    description: "Bond risk calculation rules group"
    rule-ids:
      - "bond-risk-rule-1"
      - "bond-risk-rule-2"
      - "bond-risk-rule-3"

rules:
  - id: bond-risk-rule-1
    description: "Rule 1 for Bond risk calculation rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: bond-risk-rule-2
    description: "Rule 2 for Bond risk calculation rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: bond-risk-rule-3
    description: "Rule 3 for Bond risk calculation rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "114-risk-rules.yaml"
