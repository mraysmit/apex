metadata:
  id: cftc-reporting
  type: rule-config
  version: "1.0"
  description: "CFTC reporting rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: cftc-reporting-group
    description: "CFTC reporting rules group"
    rule-ids:
      - "cftc-reporting-rule-1"
      - "cftc-reporting-rule-2"
      - "cftc-reporting-rule-3"

rules:
  - id: cftc-reporting-rule-1
    description: "Rule 1 for CFTC reporting rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: cftc-reporting-rule-2
    description: "Rule 2 for CFTC reporting rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: cftc-reporting-rule-3
    description: "Rule 3 for CFTC reporting rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "127-reporting-base.yaml"
