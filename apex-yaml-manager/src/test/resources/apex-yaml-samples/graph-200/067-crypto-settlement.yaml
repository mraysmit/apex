metadata:
  id: crypto-settlement
  type: rule-config
  version: "1.0"
  description: "Crypto settlement rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: crypto-settlement-group
    description: "Crypto settlement rules group"
    rule-ids:
      - "crypto-settlement-rule-1"
      - "crypto-settlement-rule-2"
      - "crypto-settlement-rule-3"

rules:
  - id: crypto-settlement-rule-1
    description: "Rule 1 for Crypto settlement rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: crypto-settlement-rule-2
    description: "Rule 2 for Crypto settlement rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: crypto-settlement-rule-3
    description: "Rule 3 for Crypto settlement rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "117-digital-settlement-rules.yaml"
