metadata:
  id: latam-settlement
  type: rule-config
  version: "1.0"
  description: "LATAM settlement rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: latam-settlement-group
    description: "LATAM settlement rules group"
    rule-ids:
      - "latam-settlement-rule-1"
      - "latam-settlement-rule-2"
      - "latam-settlement-rule-3"

rules:
  - id: latam-settlement-rule-1
    description: "Rule 1 for LATAM settlement rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: latam-settlement-rule-2
    description: "Rule 2 for LATAM settlement rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: latam-settlement-rule-3
    description: "Rule 3 for LATAM settlement rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "124-latam-rules.yaml"
