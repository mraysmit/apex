metadata:
  id: emir-validation
  type: rule-config
  version: "1.0"
  description: "EMIR validation rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: emir-validation-group
    description: "EMIR validation rules group"
    rule-ids:
      - "emir-validation-rule-1"
      - "emir-validation-rule-2"
      - "emir-validation-rule-3"

rules:
  - id: emir-validation-rule-1
    description: "Rule 1 for EMIR validation rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: emir-validation-rule-2
    description: "Rule 2 for EMIR validation rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: emir-validation-rule-3
    description: "Rule 3 for EMIR validation rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "126-regulatory-base.yaml"
