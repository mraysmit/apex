metadata:
  id: bond-settlement
  type: rule-config
  version: "1.0"
  description: "Bond settlement rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: bond-settlement-group
    description: "Bond settlement rules group"
    rule-ids:
      - "bond-settlement-rule-1"
      - "bond-settlement-rule-2"
      - "bond-settlement-rule-3"

rules:
  - id: bond-settlement-rule-1
    description: "Rule 1 for Bond settlement rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: bond-settlement-rule-2
    description: "Rule 2 for Bond settlement rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: bond-settlement-rule-3
    description: "Rule 3 for Bond settlement rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "111-timestamp-rules.yaml"
