metadata:
  id: bond-validation
  type: rule-config
  version: "1.0"
  description: "Bond validation rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: bond-validation-group
    description: "Bond validation rules group"
    rule-ids:
      - "bond-validation-rule-1"
      - "bond-validation-rule-2"
      - "bond-validation-rule-3"

rules:
  - id: bond-validation-rule-1
    description: "Rule 1 for Bond validation rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: bond-validation-rule-2
    description: "Rule 2 for Bond validation rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: bond-validation-rule-3
    description: "Rule 3 for Bond validation rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "100-common-validation-rules.yaml"
