metadata:
  id: clearing-validation
  type: rule-config
  version: "1.0"
  description: "Clearing validation rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: clearing-validation-group
    description: "Clearing validation rules group"
    rule-ids:
      - "clearing-validation-rule-1"
      - "clearing-validation-rule-2"
      - "clearing-validation-rule-3"

rules:
  - id: clearing-validation-rule-1
    description: "Rule 1 for Clearing validation rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: clearing-validation-rule-2
    description: "Rule 2 for Clearing validation rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: clearing-validation-rule-3
    description: "Rule 3 for Clearing validation rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "100-common-validation-rules.yaml"
