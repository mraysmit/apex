metadata:
  id: mifid-reporting
  type: rule-config
  version: "1.0"
  description: "MiFID reporting rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: mifid-reporting-group
    description: "MiFID reporting rules group"
    rule-ids:
      - "mifid-reporting-rule-1"
      - "mifid-reporting-rule-2"
      - "mifid-reporting-rule-3"

rules:
  - id: mifid-reporting-rule-1
    description: "Rule 1 for MiFID reporting rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: mifid-reporting-rule-2
    description: "Rule 2 for MiFID reporting rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: mifid-reporting-rule-3
    description: "Rule 3 for MiFID reporting rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "127-reporting-base.yaml"
