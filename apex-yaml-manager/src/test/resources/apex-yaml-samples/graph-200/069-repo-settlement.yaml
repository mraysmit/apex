metadata:
  id: repo-settlement
  type: rule-config
  version: "1.0"
  description: "Repo settlement rules"
  created-date: "2025-10-20"
  author: "APEX Test Generator"
rule-groups:
  - id: repo-settlement-group
    description: "Repo settlement rules group"
    rule-ids:
      - "repo-settlement-rule-1"
      - "repo-settlement-rule-2"
      - "repo-settlement-rule-3"

rules:
  - id: repo-settlement-rule-1
    description: "Rule 1 for Repo settlement rules"
    condition: "trade.field1 != null && trade.field1 > 0"
    severity: "ERROR"
  - id: repo-settlement-rule-2
    description: "Rule 2 for Repo settlement rules"
    condition: "trade.field2 != null && trade.field2 > 0"
    severity: "ERROR"
  - id: repo-settlement-rule-3
    description: "Rule 3 for Repo settlement rules"
    condition: "trade.field3 != null && trade.field3 > 0"
    severity: "ERROR"

dependencies:
  - "111-timestamp-rules.yaml"
