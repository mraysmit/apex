metadata:
  id: graph100-cycle-8
  name: Graph100 Cycle 8
  description: Complex cycle with multiple dependencies
  version: 0.3.0
  type: rule-config
  author: apex.demo@company.com
  created-date: 2025-10-20

rules:
  - id: cycle-rule-8
    name: Cycle Rule 8
    condition: "true"
    message: "C8 complex cycle"
    severity: INFO

rule-configurations:
  - "48-cycle-9.yaml"
  - "25-groups-common.yaml"  # Shared dependency
