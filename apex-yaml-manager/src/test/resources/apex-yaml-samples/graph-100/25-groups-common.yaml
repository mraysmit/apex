metadata:
  id: graph100-groups-common
  name: Graph100 Groups Common
  description: Common checks referenced by multiple groups
  version: 0.2.0
  type: rule-config
  author: apex.demo@company.com
  created-date: 2025-10-19
  business-domain: Trade Validation
  owner: validation.team@company.com
  tags: [rule-group, common, reusable, shared]

rule-configurations:
  - 35-rules-common.yaml

rule-groups:
  - id: common-checks
    name: Common Checks
    operator: AND
    stop-on-first-failure: false
    priority: 5
    rule-ids:
      - common-1
      - common-2
      - common-3

