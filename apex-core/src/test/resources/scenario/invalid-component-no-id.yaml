# Invalid Component - Missing ID
# Component without required 'id' field (for validation testing)

metadata:
  # id is missing - should fail validation
  name: "Invalid Component - No ID"
  version: "1.0.0"
  description: "Component missing required 'id' field"
  type: "component"
  business-domain: "testing"
  owner: "APEX Demo Team"

rule-configurations:
  - file: "BasicStageConfigurationTest-validation-rules.yaml"
    execution-order: 1
    failure-policy: "terminate"

