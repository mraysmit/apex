# Circular component A for classpath circular reference test
# References circular-b which references back to circular-a

metadata:
  id: circular-classpath-a
  name: Circular Component A
  type: component
  version: "1.0.0"
  description: References circular-b which references back to circular-a
  business-domain: testing
  owner: APEX Test Team
  criticality: low
  sla-ms: 100
  enabled: true

component-refs:
  - file: circular-classpath-b.yaml
    execution-order: 1
    failure-policy: terminate
